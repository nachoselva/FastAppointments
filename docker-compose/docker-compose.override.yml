services:
  webapp:
    build:
      context: ../src/webapp
      dockerfile: Dockerfile
      target: development
    command: npm run dev -- --host 0.0.0.0
    environment:
      - NODE_ENV=development
    ports:
      - "5173:57498"
    volumes:
      - ../src/webapp:/app:cached
      - /app/node_modules 